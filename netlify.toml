[build]
  command = "hugo version && hugo --gc --minify"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.125.7"  # Latest stable version
  NODE_VERSION = "20.0.0"
  GO_VERSION = "1.21"

[context.production.environment]
  HUGO_ENV = "production"
  HUGO_ENABLEGITINFO = "true"
